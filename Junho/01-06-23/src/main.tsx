import React from 'react'
import ReactDOM from 'react-dom/client'
import CreateRecord from './Ex1'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <CreateRecord/>
  </React.StrictMode>,
)
